# Basis-URL des Auth-Backends.
# Leer lassen = Mock-Modus (localStorage, kein Backend noetig).
# Beispiel fuer das vorhandene FastAPI-Backend: http://localhost:8000
VITE_AUTH_API_BASE=

# Anzeigename der Anwendung (Branding).
VITE_APP_NAME=KeyAuth
